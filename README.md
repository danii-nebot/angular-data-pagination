AngularJS Data Pagination assignment
=======================

- The view will display analytic reporting data related to location data. An HTML table and a pagination system should be provided to the end-user.

- On the left is the page length selector, so the user can display 10, 20 or 30 rows to a page.

- On the right is the page selector to navigate to a specific page or the last page.
Below is the reporting data.
